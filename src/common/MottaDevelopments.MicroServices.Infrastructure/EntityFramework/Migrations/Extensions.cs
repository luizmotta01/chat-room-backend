using System;
using System.Threading.Tasks;
using Microsoft.Extensions.DependencyInjection;

namespace MottaDevelopments.MicroServices.Infrastructure.EntityFramework.Migrations
{
    public static class Extensions
    {
        //public static async Task<IServiceProvider> MigrateDbContext<TDbContext>(this IServiceProvider serviceProvider) where TDbContext : DbCon
        //{
        //    using var scope = serviceProvider.GetService<IServiceScopeFactory>().CreateScope();

        //    var context = scope.ServiceProvider.GetRequiredService<TDbContext>();

        //    await context.Database
        //}
    }
}